# The POC is intentionally local-only by default.
CARCHIEF_BIND_ADDRESS=127.0.0.1
CARCHIEF_POC_PORT=8080

# Optional: required only when starting the Compose "ai" profile.
# Never commit a real key.
GEMINI_API_KEY=
